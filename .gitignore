# Node / frontend
node_modules/
frontend/node_modules/
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual env
.venv/
venv/

# Untrusted local/Vercel storage — never commit
.vercel

# Secrets — never commit
.env
.env.backup
.env*
data/*.db
data/*.db-journal
data/*.sqlite
backend/data/*.db
backend/data/*.db-journal
backend/data/*.sqlite
logs/*.log
logs/*.log.*
fyersDataSocket.log
*.log
# Railway worker config may contain real DB URLs / tokens
railway-worker.toml

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local inspection artifacts (not part of the project)
trading-system-audit/
vibetrading-inspection/

# Embedded git submodules removed from index (not needed for deployment)
# These dirs exist locally for reference but must not be tracked as gitlinks
agency-agents/
algo_trading_strategies_india/
bhav/
learn-trading-from-x/
nifty-daily-moves-and-gaps/

# AI Office local SQLite databases (test artifacts)
.ai-office/*.db
.ai-office/*.db-journal

# OS
Thumbs.db
.DS_Store
