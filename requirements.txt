# Day 1 core dependencies (pinned to tested versions)
pandas==3.0.5
numpy==2.4.6
SQLAlchemy==2.0.52
python-dotenv==1.2.3
requests==2.31.0
tabulate==0.10.0
pydantic==2.13.4
pytest==9.1.1
websocket-client==1.7.0

# fyers-apiv3 is OPTIONAL now (only needed for the legacy FYERS adapter kept in Phase 14).
# Install it only if you still use the FYERS provider: pip install fyers-apiv3==3.1.16
